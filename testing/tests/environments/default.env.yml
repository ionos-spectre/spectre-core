cert: &cert ./resources/<root_cert>.cer
http:
  web_api:
    base_url: http://localhost:4567/api/v1/
    basic_auth:
      username:
      password:
    keystone:
      url: https://localhost:4567/keystone/main/v3/
      username:
      password:
      project: some_project
      domain: some_domain
      # cert: *cert
ftp:
  sftp-server:
    host: access-5000336343.ac1-product.lan
  ftp-server:
    host: localhost
mysql:
  developer:
    host: localhost
    username:
    password:
# ssh:
#   <ssh_client_name>:
#     host: <hostname>
#     username: <username>
#     password: <password>
